{{ blok._editable }}
<a href="{{ blok.link }}" class="btn btn-icon left-icon btn-fill">
  <svg class="ico">
    
    <use xlink:href="#{{ blok.icon.icon }}"></use>
  </svg>
  <span class="btn-text">{{ blok.label }}</span>
</a>